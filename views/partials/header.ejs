<!DOCTYPE HTML>
<html lang="en">
  <head>
    <title><%=title%></title>
    <% if (DEV_MODE) { %>
      <link rel="stylesheet" href="/css/site.min.css" />
    <% } else { %>
      <link rel="stylesheet" href="/pulsar-documentation/site.min.css" />
      <% //This css path is only needed because my account for GitHub already has a root page %>
    <% } %>
  </head>
  <body class="inverse">
